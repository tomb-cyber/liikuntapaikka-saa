/* import axios from 'axios'

const getAll = () => {
    const request = axios.get('/saatiedot')
    return request.then(response => {
        console.log(response.status)
        return response.data
    })
}

const exported =  { getAll }

export default exported */